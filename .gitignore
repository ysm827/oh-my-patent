node_modules/
dist/
*.js
*.d.ts
!*.test.ts
!jest.config.*
coverage/
*.tmp
*.log
.DS_Store
Thumbs.db
.vite/
*.cache

# Test temporary directories
.test-*
.audit-temp/
.audit-tmp/
.audit_temp/
.audit_tmp/
*-temp/
*-tmp/

# Development temporary files
findings.md
task_plan.md
progress.md
.claude/
.workbuddy/

# Generated adapter output. It is reproducible from tracked inputs, so it is no
# longer versioned (DEC-1 in specs/001-review-remediation). Regenerate with:
#   npm run build && node dist/cli.js adapt generate
plugins/

# Audit reports directory (keep locally for reference, don't commit)
.audit-reports/

# npm package files
*.tgz

# Accidentally created directories (from CLI testing)
--help/
--*/
